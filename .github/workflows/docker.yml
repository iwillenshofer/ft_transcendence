name: Docker Image CI

on:
  push:
    branches: [ "bug_cli" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build ./srcs/frontend --file Dockerfile --tag my-image-name:$(date +%s)
